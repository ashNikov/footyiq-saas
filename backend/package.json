{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "build": "tsc",
    "zip:prediction": "npm run build && cd dist && zip -r ../prediction.zip ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^20.19.25",
    "aws-lambda": "^1.0.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
